---
name: 企业调研
description: >
  企业调研 skill v2.1 — 标准化企业背景调研工作流。收到公司全称后，按搜索路由+
  多维并行调研+来源可靠性分级完成信息采集与交叉验证，输出结构化调研简报。
  集成AnySearch(增强层)+Scrapling反爬+浏览器渲染+WebSearch搜索+文本清洗。
  AnySearch 作为性能增强层（有它更快，无它也跑）。
  可独立使用，也可作为其他Skill的Stage 0。
  触发词：企业调研、查公司背景、调研公司、公司调研、客户调研、企业背调
agent_created: true
---

# 企业调研

> 标准化、可复用的企业背景调研工作流。收到公司全称后，按规则完成调研、验证、产出简报。
> 可独立使用于任何项目，也可嵌入其他 Skill 作为前置调研阶段。

---

## 一、技能定位

**做什么**：
- 收到公司全称，完成从信息采集到结构化简报输出的完整调研
- 按搜索意图选择最合适的工具
- 按来源可靠性分级（A/B/C/D）标注每条信息
- 按交叉验证规则确保关键事实准确
- 输出可直接使用的《企业调研简报》

**不做什么**：
- 不做深度行业研究（超出企业基本信息范围）
- 不做竞品对标分析
- 不联系目标公司

**适用场景**：
- 接到新客户/新项目，需要快速了解对方背景
- 培训项目售前调研（配合师资选聘流程）
- 商务谈判前的客户背景摸底
- 供应商/合作伙伴尽职调查初筛

---

## 二、核心原则

1. **按意图选工具，不一股脑全搜** — 不同搜索目的用不同工具（见路由表）
2. **信息必须分级标注** — 每条结论带 A/B/C/D 等级，可追溯可验证
3. **关键事实必须交叉验证** — 1个A级 + 1个B级 或 2个B级 才采信
4. **矛盾保留，不强行调和** — 不同来源冲突时标注双方
5. **宁可标注局限，也不编造信息** — 搜不到就写"信息有限"

---

## 工具说明

本技能使用以下工具进行信息采集，按搜索意图优先级选用：

| 层级 | 工具 | 类型 | 适用场景 | 可靠性 |
|:---:|:---|:---|:---|:---:|
| **⚡ 增强层** | **AnySearch** | 自有API搜索引擎 | 通用搜索、批量搜索（速度优先） | ★★★★★ |
| **🔧 基础层** | **WebSearch** | 系统内置搜索 | 兜底搜索、关键词发现、URL定位、企查查工商摘要 | ★★★★ |
| **🔧 基础层** | **Scrapling** | 反爬网页抓取 | 官网深度提取、动态页面（配合浏览器） | ★★★★ |
| **📡 补充层** | **RSS订阅** | 行业动态跟踪 | 持续监测行业新闻、竞对动态 | ★★★ |
| **📡 补充层** | **多渠道** | 社交/论坛搜索 | 口碑评价、员工反馈（V2EX/小红书/脉脉等） | ★★ |

> 💡 **AnySearch 定位：性能增强层** — 有它搜索更快（1.8s vs 3-5s），批量搜索效率更高（并行5组），输出更整洁（结构化JSON）。
> 但流程不依赖它：AnySearch 不可用时，自动降级到 WebSearch，流程不受影响。
> AnySearch CLI 路径：`python skills/anysearch/scripts/anysearch_cli.py`

### 浏览器安装（增强Scrapling反爬能力）

Scrapling 配合浏览器可绕过较复杂的反爬（如 Cloudflare），需以下操作：

```bash
# 安装浏览器组件（约294MB）
python -m playwright install chromium

# 安装后使用方式
from scrapling import StealthyFetcher
StealthyFetcher.adaptive = True
page = StealthyFetcher.fetch('https://company.com',
    headless=True, browser='chromium', network_idle=True)
```

> ⚠️ 注意：企查查/天眼查/爱企查等工商数据平台使用了专业 WAF 防护，
> 即使使用浏览器也无法穿透。工商信息建议通过 WebSearch 搜索摘要获取。

### 文本清洗规则（Scrapling 专用）

Scrapling 提取的网页内容会混入 JS/JSON/模板噪声，必须过滤：

```python
NOISE_PATTERNS = [
    'function', 'var ', 'const ', 'let ', 'window.',
    '__NUXT', 'ShallowReactive', 'Reactive',
    'document.', 'undefined', 'null', 'true', 'false',
    'Object.', 'Array.', 'console.', 'require',
]
def clean_page_text(texts):
    clean = []
    for t in texts:
        t = t.strip()
        if len(t) < 4:
            continue
        if any(p in t[:50].lower() for p in NOISE_PATTERNS):
            continue
        clean.append(t)
    return clean
```

> 实测验证：降噪率约22.3%，可将175个原始节点清洗为136个可用节点。

---

## 三、工作流程

```
    输入：公司全称
         ↓
  1. 搜索路由分类
     （根据意图选工具，分5个场景）
         ↓
  2. 四维并行调研
     A-基本面 / B-行业竞争 / C-口碑 / D-动态
         ↓
  3. 来源可靠性分级
     （A/B/C/D四级标注）
         ↓
  4. 交叉验证
     （关键事实确认/冲突处理）
         ↓
  5. 调研Review检查点
     （展示质量摘要，确认后再输出）
         ↓
    输出：企业调研简报
```

---

## 四、详细执行步骤

### Step 1：搜索路由分类

**Step 1.0 前置：工具可用性检测**
在开始搜索之前，先快速检测各工具是否可用：

```python
# 检测 AnySearch CLI 是否可达
import subprocess, os
anysearch_ok = os.path.exists(r'skills/anysearch/scripts/anysearch_cli.py')
# 检测 Scrapling 是否可导入
try:
    from scrapling import Fetcher
    scrapling_ok = True
except ImportError:
    scrapling_ok = False
```

检测结果决定后续工具选择：
- AnySearch 可用 → 优先调用（⚡增强层）
- AnySearch 不可用 → 直接跳到 WebSearch（🔧基础层）
- Scrapling 不可用 → 跳过官网深度提取，只用 WebSearch + 百度百科

**Step 1.1 搜索路由**

| 搜索意图 | 优先工具（⚡增强->🔧基础） | 业务规则 | fallback路径 |
|---------|-------------------------|---------|-------------|
| **查官网/公司简介** | **AnySearch → WebSearch找URL → Scrapling提取** | AnySearch快速搜官网URL（1.8s），命中后用Scrapling提取 | AnySearch失败→WebSearch；Scrapling失败→百科替代 |
| **查工商/资质/股权** | **AnySearch/WebSearch 搜企查查/百度百科摘要** | 企查查有WAF，AnySearch/WebSearch获取公开摘要即可 | 标注"需手动查询" |
| **搜行业新闻/动态** | **AnySearch批量搜索 → WebSearch + RSS补充** | AnySearch并行搜多组关键词，效率最高；不够再用WebSearch逐条补 | → 换同义词重试 |
| **搜财报/公告/战略** | **AnySearch垂直域 + WebSearch + Scrapling官网** | AnySearch的finance域可搜结构化财务数据 | → RSS行业订阅补充 |
| **搜社交口碑/评价** | 多渠道（V2EX/小红书/Twitter/脉脉） | 只作信号参考，不决策 | → 标注"信息有限" |
| **搜竞品/产业链** | **AnySearch批量搜索（并行搜多公司）** | 同时搜索目标公司及主要竞品 | → WebSearch逐家搜索 |

> **AnySearch vs WebSearch 选择原则**：AnySearch 可用时优先（更快+结构好），不可用时自动降级到 WebSearch。两者搜索策略一致，只是速度和输出格式不同。
> **超时处理**：每个搜索请求设 15s 超时。超时后直接走 fallback 路径，不阻塞流程。
> 某条路径搜索失败 → 按fallback重试，再失败 → 标注"信息有限"

---

### Step 2：四维并行调研

从4个维度并行搜索，各自独立输出，最后汇总：

```
维度A：公司基本面
  ├── 提取：全称/注册资本/成立时间/总部/股权结构/法人/企业性质
  ├── 工具：⚡AnySearch（优先，1.8s）→ 🔧Scrapling官网+WebSearch企查查摘要（兜底）
  └── 可信度预期：A级

维度B：行业与竞争
  ├── 提取：行业地位/主要竞对/市场排名/政策环境/产业链位置
  ├── 工具：⚡AnySearch批量搜索（并行5组）→ 🔧WebSearch + RSS（补充）
  └── 可信度预期：B级

维度C：企业文化与口碑
  ├── 提取：员工评价/企业文化/管理风格/行业声誉
  ├── 工具：多渠道（V2EX/小红书/脉脉等）
  └── 可信度预期：C-D级（仅供参考）

维度D：近期动态与战略
  ├── 提取：战略调整/重大事件/财报/人事变动/投资并购
  ├── 工具：⚡AnySearch垂直域 → 🔧WebSearch + Scrapling官网新闻页（兜底）
  └── 可信度预期：A-B级
```

> **信息源黑名单**：知乎、百度知道、微信公众号洗稿文 → 不作为任何维度的来源
> **文本清洗规则**：Scrapling 爬取的网页内容会混入JS/JSON/模板噪声，使用 [公司调研-Scrapling] skill 中的 `clean_page_text()` 函数过滤

---

### Step 3：来源可靠性分级

每条调研结论按以下分级标注：

| 等级 | 类型 | 示例 | 说明 |
|:--:|------|------|------|
| **A级** | 官方一手 | 公司官网、上市公司公告/年报、国家企业信用公示系统、天眼查/企查查工商数据 | 可直接引用，但注意更新日期 |
| **B级** | 权威二手 | 主流财经媒体(36氪/界面/证券时报)、百度百科、行业协会报告、政府公开数据 | 可信度高，建议与A级交叉验证 |
| **C级** | 普通二手 | 行业自媒体、微信公众号、知乎专栏、地方新闻报道 | **需要两条以上不同来源印证** |
| **D级** | 碎片信息 | 小红书/微博/Twitter员工吐槽、匿名论坛、短视频评论 | 只作"信号参考"，不作为决策依据 |

---

### Step 4：交叉验证

**关键事实**（企业性质/营收规模/战略方向）：
```
至少需要 1个A级 + 1个B级 或 2个不同B级 同时指向同一结论
如果只有C级来源 → 标注"待确认"，在备注中说明
```

**非关键事实**（近期动态/行业排名）：
```
至少需要 1个B级 或 2个C级
否则标注"信息有限"
```

**冲突信息处理**：
```
A级 > B级 > C级 > D级
同级来源冲突 → 标注双方观点，不强行选择
```

**信息标注格式**：

```
信息点：{内容}
来源：{来源链接/名称}（{A/B/C/D级}）
验证：{已交叉验证 / 单一来源 / 待确认}
时间：{采集日期}
```

---

### Step 5：调研Review 检查点

> 4个维度调研完成后，**暂停并展示调研质量摘要**，在输出简报前先做一轮质量审查。

```
┌──────────────────┬──────────┬──────────────────────────────┐
│ 调研维度         │ 来源数量  │ 关键发现                      │
├──────────────────┼──────────┼──────────────────────────────┤
│ A-公司基本面     │ X个      │ 注册资本XX，企业性质为XX...    │
│ B-行业与竞争     │ X篇      │ 细分领域排名第X，主要竞对...   │
│ C-文化口碑       │ X条      │ 信息有限/员工提到XX问题         │
│ D-近期动态       │ X条      │ 20XX年X月发生XXX...            │
├──────────────────┼──────────┼──────────────────────────────┤
│ 矛盾点           │ X处      │ A源说XX，B源说YY              │
│ 信息不足维度     │ XX       │ 标注"信息有限"                 │
│ 调研总来源数     │ X个      │ A级X个 / B级X个 / C级X个       │
└──────────────────┴──────────┴──────────────────────────────┘
```

**审查规则**：
- 矛盾点 ≥ 2处 → 在简报中单独列出"待核实信息"
- A级来源 = 0 → 标注"缺乏官方一手信息"
- 所有关键事实均为C级或以下 → 标注"调研结果准确度有限"
- 通过 → 输出简报；不通过 → 对不足维度补充搜索

---

### 输出：企业调研简报

可用 `references/generate_brief.py` 脚本一键生成（需先准备调研数据JSON）：

```bash
python references/generate_brief.py --data 调研数据.json > 调研简报.md
```

简报标准模板：

```markdown
# {公司全称} 企业调研简报

**调研日期**：{YYYY-MM-DD}
**调研方式**：多渠道并行调研（Scrapling / WebSearch / RSS / 其他）
**信息可靠性**：A级{X}条 / B级{X}条 / C级{X}条 / D级{X}条

## 一、公司基本信息
- 全称：
- 企业性质：
- 注册资本：
- 成立时间：
- 总部所在地：
- 主营业务：
- *来源标注与验证状态*

## 二、行业与竞争定位
- 行业地位：
- 主要竞对：
- 政策环境：
- *来源标注与验证状态*

## 三、近期动态（近12个月）
- 战略调整：
- 重大事件：
- 业绩/财报：
- *来源标注与验证状态*

## 四、企业文化与口碑（参考）
- 员工评价：
- 行业声誉：
- *信号等级：仅供参考*

## 五、待核实信息
- {冲突/不明信息，标注需要向客户确认的内容}

## 六、备注
- 调研局限性说明
```

---

## 五、与其他技能的关系

```
┌─────────────────────────────────────────────────┐
│  企业调研（本技能）                                │
│  ├── 性能增强层：AnySearch（快速+批量+垂直）       │
│  ├── 基础数据层：                                  │
│  │    ├── Scrapling（反爬官网提取+浏览器渲染）       │
│  │    ├── WebSearch（系统内置搜索）                │
│  │    └── RSS订阅 / 多渠道（补充）                 │
│  └── 可被其他 Skill 调用                          │
│       ├── 培训师资选聘全流程 → Stage 0 调用        │
│       ├── 商务谈判前客户摸底                      │
│       └── 合作伙伴尽职调查                        │
└─────────────────────────────────────────────────┘
```

---

## 六、常用命令

| 命令 | 功能 |
|------|------|
| `调研 {公司全称}` | 启动完整调研流程 |
| `{公司全称} 快速` | 快速模式（只做A+B维度，跳过C+D） |

---

## 七、常见问题

### Q：搜不到公司官网怎么办？
A：先确认公司全称是否正确。用简称+行业关键词重试搜索。如果仍然找不到，标注"官网未找到"，使用百度百科/企查查等二级来源。

### Q：企查查/天眼查有反爬怎么办？
A：用 Scrapling 的 StealthyFetcher 模式（自适应+浏览器渲染）。如果仍然失败，标注"工商信息需手动查询"。

### Q：搜到的信息互相矛盾？
A：保留矛盾，标注双方来源等级。在简报的"待核实信息"中列出，后续向客户确认。

### Q：Scrapling 提取的内容有很多JS代码/噪声？
A：使用 `clean_page_text()` 函数过滤（见工具说明章节的文本清洗规则）。已实测验证可有效过滤。

### Q：官网URL输入后打不开/无效？
A：先 WebSearch 搜索公司全称，找到正确官网地址，再用 Scrapling 提取。搜索引擎结果通常比手动猜测更准确。

### Q：AnySearch 和 WebSearch 有什么区别？先调用哪个？
A：AnySearch 是**性能增强层**（更快1.8s、输出结构整洁、支持批量并行），优先调用。AnySearch 不可用时自动降级到 WebSearch，不影响流程。两者搜索策略一致。底层参考：AnySearch基于自有API（anysearch.com），WebSearch基于系统内置搜索引擎。

### Q：搜索超时了怎么办？
A：每个搜索请求默认 15s 超时。超时后自动走 fallback 路径（如 AnySearch→WebSearch，WebSearch→RSS/百科），不阻塞整个调研流程。如果所有路径都超时，标注"搜索服务暂不可用"，输出已有信息。
